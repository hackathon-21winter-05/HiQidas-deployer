{
	admin off
	servers {
		timeouts {
			read_body 0
			read_header 0
			write 0
			idle 0
		}
	}
}

:80

encode zstd gzip
root * /usr/share/caddy

handle /api/* {
	reverse_proxy localhost:7070
}

handle {
	file_server {
		precompressed zstd gzip
	}
	try_files {path} /index.html
}
